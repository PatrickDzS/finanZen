import * as React from 'react';

// This is a placeholder component.
// The file was empty, which can cause build issues. This placeholder ensures
// the file is a valid React component module, even if it's not currently
// rendered by the application.

const ProactiveAIAssistant: React.FC = () => {
  return null; // This component renders nothing.
};

export default ProactiveAIAssistant;
